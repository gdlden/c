#include<stdio.h>
#include<stdlib.h>

typedef struct LNode LNode;

//定义结构体积，作为链表的每一个节点的数据结构
struct LNode
{
    //数据域，保存该节点的数据
    int data;
    //指针域，保存了下一个节点的地址
    struct LNode* next;
};



int main(int argc, char const *argv[])
{
    
    return 0;
}
